//67
// prime
#include<stdio.h>
int i;
int is_prime(int n)
{
    for(i=2;i<n;i++)
    {
        if(n%i==0)
        return 0;
    }
        return 1;
}

int main()
{
    int n;
    scanf("%d",&n);
    if(is_prime(n)==1)
    printf("prime\n");
    else
    printf("composite\n");
    
    return 0;
}
