/* given all numbs from 1 to n execpt one numbs.have to find that numbs.
5
3 4 1 2
5 */
#include<stdio.h>
int main()
{
    int i,x,n;
    int N=300;
    int arr[N];
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
        scanf("%d",&x);
        arr[x]=1;
    }
    for(i=1;i<=n;i++)
    {
        if(arr[i]!=1)
        printf("%d",i);
    }
    //  printf("%d ",arr[i]);
}
