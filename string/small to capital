/* ibrahim molla
IBRAHIM MOLLA */
#include<stdio.h>
int main()
{
    int i;
    char ch[100];
    fgets(ch,sizeof(ch),stdin);
    for(i=0;ch[i]!='\0';i++)
    {
        if(ch[i]>='a' && ch[i]<='z')
        ch[i]-=32;
    }
    puts(ch);
}
