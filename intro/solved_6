/* take two positive int N & M as input & print all the numbs from 1 to N that are divisible by M.
->10 3
<-3 6 9
->15 4
<-4 8 12 */
#include<stdio.h>
int main()

{
    int n, m, i;
    scanf("%d%d", &n, &m);
    for(i=1; i<=n; i++){
        if(i%m == 0){
            printf("%d ", i);
        }
    }
    printf("\n");
    return 0;
}
